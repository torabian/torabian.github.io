{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\Module3.yml$",
        "cmd": "${workspaceFolder}/artifacts/pkp-server/pkp gen gof --relative-to ${workspaceFolder} --def ${file} --no-cache true --gof-module github.com/myorg/mynewbackend && ./artifacts/pkp-server/pkp gen module3spec --out ${workspaceFolder}/.jsonschemas/${relativeFile}"
      },
      {
        "cmd": "emi compile --path ${relativeFile}",
        "match": "\\.emi.yml$"
      }
    ]
  },
  "yaml.schemas": {
    ".jsonschemas/module3-json-schema.json": ["*Module3.yml"],
    "./.vscode/emi-module-spec.json": ["*.emi.yml", "*.emi.yaml"]
  }
}
