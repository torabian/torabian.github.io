# PKP API SDK Project

This library contains the sdk generated based on the API from PKP (Poland train provider).
It's supporting all their features as of 2026, August. This is not official sdk, better to check the
definitions with most recent changes.
