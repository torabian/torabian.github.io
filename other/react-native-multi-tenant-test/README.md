# React native whitelabling demo

This is a project that shows how you can completely make a react native project to be whitelable-able :)

It would download every details from API call, and remote content is placed in `sdk` folder, which in real scenario could be totally
and backend.

Project is standard react native expo project, no dependencies or tricks needed to run.
